<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<data>
        <record id="action_view_all_invoices" model="ir.actions.act_window">
            <field name="name">Invoices</field>
            <field name="res_model">account.move</field>
            <field name="view_mode">tree,kanban,form,activity</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                Create a customer invoice
              </p><p>
                Create invoices, register payments and keep track of the discussions with your customers.
              </p>
            </field>
        </record>
        <record id="action_view_all_invoices_view_tree" model="ir.actions.act_window.view">
            <field name="view_mode">tree</field>
            <field name="view_id" ref="account.view_out_invoice_tree"/>
            <field name="act_window_id" ref="action_view_all_invoices"/>
        </record>

        <!-- Specify the form view for the action -->
        <record id="action_view_all_invoices_view_form" model="ir.actions.act_window.view">
            <field name="view_mode">form</field>
            <field name="view_id" ref="account.view_move_form"/>
            <field name="act_window_id" ref="action_view_all_invoices"/>
        </record>
	</data>
</odoo>